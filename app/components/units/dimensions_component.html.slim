p Bounds: #{unit_width.round(1)} (w) x #{unit_height.round(1)} (h) x #{unit_depth.round(1)} (d)
p Shelf opening: #{shelf_opening.round(1)}
p Vertical offsets: #{offset_top} (top), #{offset_bottom} (bottom)
